<% flash.each do |name, msg| %>
  <div class="alert <%= bootstrap_class_for(name) %> p-4 mt-4">
    <%= msg %>
  </div>
<% end %>




<h1 class="font-bold text-4xl">2段階認証</h1>
<p>補足:2段階認証が成功したら、ログアウトする迄有効なアプリ内ファイルのダウンロードパスワードがメール(mitsui.seimitsu.iatf16949@gmail.com)で届きます。</p>

<%= form_tag two_step_verifications_path, method: :post do %>
  <div class='field'>
    <img src="<%= @qr_code %>" alt="QR Code">
  </div>

  <div class='field'>
    <%= label_tag :otp_attempt, '認証' %> <i style="opacity: 0.5;">(ワンタイムパスワード入力欄)</i><br />
    <%= text_field_tag :otp_attempt, '', placeholder: 'ワンタイムパスワード入力' %>
  </div>

  <div class='actions'>
    <%= submit_tag '認証',class:"btn btn-sm" %>
  </div>
<% end %>
